using Arch.Core;

namespace MUD.Rulesets.D20.Components
{
    // Tag: Entity is alive but cannot act/move
    public struct UnconsciousComponent { }

    // Tag: Entity is dead (permanently)
    public struct DeadComponent { }
}